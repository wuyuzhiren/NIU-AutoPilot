# 小牛电动车「靠近自动开机」助手 v2.1（HTTP 远程命令版）

## 一、这是什么

一个安装在手机（安卓）上的 App：
1. **靠近自动开机**：后台定时扫描你车辆的蓝牙信号，信号够强就自动调用**官方远程接口**发送「开机」命令，副账号也能用、不用蓝牙连接。
2. **离开自动关机**（可选）：走远后信号变弱，自动发送「关机」。
3. **手动控制**：一键 开机 / 关机 / 开坐桶 / 查状态。

**v2.1 改进**：蓝牙设备改为**从扫描列表里手动选择**（不再按服务 UUID 过滤，兼容性更好）；参数全部改为**滑块**调节并显示对应距离；移除了锁车/解锁按钮。

## 二、官方远程命令（已实测确认）

| 功能 | 命令 | 实测 |
|---|---|---|
| 开机 | `acc_on` | ✅ |
| 关机 | `acc_off` | ✅ |
| 开坐桶 | `cushion_lock_on` | ✅ 不用先开机 |

## 三、安装

1. 把 `小牛靠近开机助手-v2.1.apk` 传到手机安装（允许未知来源）。
2. 首次打开允许**蓝牙、定位、通知**权限。

## 四、首次设置（3步）

1. **填账号**：手机号 `19204013070`、密码、车辆SN（`2DFSH2PATC15000AG17E`，官方App"关于"里查）。点「查状态」能返回"开机/关机"即正确。
2. **选择车辆蓝牙**：点「扫描并选择我的车辆」→ 等5秒 → 在列表里选你的车（通常名字带 **NIU**，连不上也没关系，选它就行）。
3. **启动自动模式**：勾选「靠近自动开机」→ 点「启动自动模式」→ 顶部出现通知即开始工作。

## 五、参数滑块说明（含距离参考）

所有参数用滑块拉动，界面实时显示：

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 扫描间隔 | 3~30 秒 | 5 秒 | 多久扫一次蓝牙；越大越省电 |
| 靠近阈值 | -90~-40 dBm | -60 | 信号强于它就开机；右拉=更灵敏 |
| 离开阈值 | -95~-45 dBm | -80 | 信号弱于它就关机；左拉=更灵敏 |
| 防抖冷却 | 5~62 秒 | 30 秒 | 开机后多久内不重复开机 |

**RSSI 大概距离参考（滑块上会同步显示）：**

| RSSI | 大概距离 |
|---|---|
| -45 | 0.5 米内（贴车） |
| -52 | 约 1 米 |
| -58 | 约 2 米 |
| -64 | 约 3 米 |
| -70 | 约 5 米 |
| -76 | 约 8 米 |
| -82 | 约 12 米 |
| 更弱 | 15 米以上 |

**调法**：想"走到车边 2 米就开机"→ 靠近阈值拉到 -58 左右；想"只有凑到 1 米内才开机"→ 拉到 -50 左右。以 App 顶部实时显示的 RSSI 为准微调。

## 六、小米/红米后台保活（重要）

1. 设置 → 应用设置 → 应用管理 → 小牛靠近开机助手 → **省电策略 → 无限制**。
2. 同上位置 → **自启动 → 打开**。
3. 最近任务里把本 App **下拉锁定**。
4. 蓝牙保持开启、定位权限保持允许。

## 七、重新打包

源码在 `E:\小牛自用\NIUAutoPilot`：
```
set JAVA_HOME=C:\Users\Administrator\AppData\Local\Temp\niu_work\asetup\jdk\jdk-17.0.20.1+1
set ANDROID_HOME=C:\Users\Administrator\AppData\Local\Temp\niu_work\asetup\sdk
C:\Users\Administrator\AppData\Local\Temp\niu_work\asetup\gradle\gradle-8.7\bin\gradle.bat --no-daemon assembleDebug
```
产物：`app\build\outputs\apk\debug\app-debug.apk`

# 小牛电动车「靠近自动开机」助手 使用说明

## 一、这是什么

一个安装在手机（安卓）上的 App，功能：

1. **靠近自动开机**：手机后台定时扫描你车辆的蓝牙信号，信号强度（RSSI）够强（说明你靠近车）就自动连接并发送「开机」指令，实现不用钥匙、不用主账号的靠近自动开机。
2. **离开自动关机**（可选）：走远后信号变弱，自动发送「关机」指令。
3. **手动控制**：一键开机 / 关机 / 开坐桶。
4. **探测模式**：扫描并显示车辆蓝牙及信号强度，可手动发送任意指令测试。

## 二、安装

1. 把 `NIUAutoPilot-debug.apk` 传到手机（微信/QQ/数据线均可）。
2. 手机上点击安装。若提示"未知来源"，允许安装即可。
3. 首次打开会请求**蓝牙、定位、通知**权限，全部允许。
4. 打开手机蓝牙。

## 三、快速开始

1. 打开 App，点「扫描」，等几秒。
2. 列表里应出现你的车（设备名通常是 **NIU** 或 **NIU Link** 开头，服务 UUID `8ec94e30-...`）。
3. 点「连接」→ 连接成功后点「开机」「关机」「开坐桶」测试。
4. 测试可用后，勾选「靠近自动开机」，设置好阈值，点「启动自动模式」，即可锁屏后台运行。

## 四、参数说明

| 参数 | 含义 | 建议 |
|---|---|---|
| 扫描间隔(秒) | 多久扫一次蓝牙 | 5~10 秒 |
| 靠近阈值(dBm) | 信号强于它就触发开机 | -60（约 1~3 米）|
| 离开阈值(dBm) | 信号弱于它就触发关机 | -80 |
| 冷却(秒) | 开机后多久内不再重复开机 | 60 |
| 服务 UUID | 车辆蓝牙服务 | `8ec94e30-f315-4f60-9fb8-838830daea50`（可改）|
| 写特征值 UUID | 发指令用的通道 | `8ec94e32-f315-4f60-9fb8-838830daea50` |
| 通知特征值 UUID | 收数据用的通道 | `8ec94e31-f315-4f60-9fb8-838830daea50` |
| 开机/关机/开坐桶指令 | 发送的指令帧(Hex) | 见下方"指令"章节 |

## 五、指令（重点）

本 App 的指令来自对官方 App（base.apk）的逆向分析：

- 小牛蓝牙协议帧格式：`5A A5 [长度 2字节] [命令字节] [数据...]`
- 服务 UUID：`8ec94e30-f315-4f60-9fb8-838830daea5X`
- 写特征值：`8ec94e32-f315-4f60-9fb8-838830daea5X`
- 从官方 App 字符串池提取到的候选帧：
  - `5aa5000a050900000000011cd396`
  - `5aa5000a060900000000011cd396`
  - `5aa5000a06090000000000359096`

⚠️ 由于官方 App 使用网易易盾加固，**开机/关机/开坐桶的最终指令字节在加密代码里**，App 里预填的是候选帧。如果你的车型实测不生效，按下面方法抓取正确指令：

### 方法A：nRF Connect 抓包（推荐，10分钟）
1. 找一位有小牛主账号的朋友，在他的手机上登录小牛 App。
2. 你手机上安装 nRF Connect（应用商店/GitHub 都有）。
3. 靠近车辆，打开 nRF Connect，找到你的车（NIU Link 开头），连接。
4. 打开 App 里的「开机」「关机」「开坐桶」，同时盯着 nRF Connect 里 `8ec94e32` 特征值的写入记录。
5. 把抓到的 Hex 分别填入本 App 对应的三个指令框，保存即可。

### 方法B：本 App 探测模式
1. 连接车辆后，在「自定义指令Hex」里逐个尝试候选帧，观察车辆反应（仪表灯/坐桶弹起）。
2. 有反应的帧就是正确指令，填进对应按钮。

### 方法C：旧协议指令（部分车型适用）
部分老款/无系统车型使用 `5A ... A5` 帧格式（头码5A、尾码A5、中间XOR校验）。这类可直接构造：
- 开机：`5A C1 10 01 00 00 00 00 00 00 00 00 00 00 8A A5`
- 关机：`5A C1 10 81 00 00 00 00 00 00 00 00 00 00 0A A5`
（仅供参考，具体以你车型为准）

## 六、工作原理（官方 App 同款机制）

从官方 App 逆向确认：小牛"靠近自动开机"就是
- 车辆蓝牙一直广播（iBeacon / 服务 UUID `8ec94e30`）
- App 后台扫描，取 RSSI
- RSSI ≥ 阈值（靠近）→ 连接 → 发开机指令
- RSSI < 阈值（离开）→ 发关机指令

本 App 复刻了这套逻辑，只是不需要主账号登录。

## 七、常见问题

- **扫描不到车**：确认蓝牙已开、权限已给；靠近车辆；检查设备名过滤是否误填。
- **连接上但指令无效**：大概率是指令帧不对，用第五节方法抓正确指令。
- **后台被系统杀掉**：小米/红米需要到"设置→应用管理→本App→省电策略→无限制"，并锁定后台。把 App 加到系统"自启动"白名单。
- **扫描耗电**：把扫描间隔调到 10 秒以上。

## 八、重新打包

想改代码重新打包？源码在 `E:\小牛自用\NIUAutoPilot`，已配置好 Gradle 环境，
命令行执行：
```
gradle assembleDebug
```
产物在 `app\build\outputs\apk\debug\`。

# 小牛官方 App 逆向分析报告（base.apk）

## 一、App 基本信息
- 包名：`com.niu.cloud`（小牛电动）
- 加固方式：**网易易盾（NESEC，com.netease.nis.wrapper）**
- 结论：`classes.dex` 是加壳壳代码（仅 1063 字符串 / 30 类），真正的业务代码运行时才解密。
  但**字符串池未加密**，以下协议信息即从明文字符串池提取。

## 二、BLE 蓝牙协议（已确认）

### 服务与特征值
| 项目 | UUID | 说明 |
|---|---|---|
| 服务 UUID | `8ec94e30-f315-4f60-9fb8-838830daea5X`（X=0/1/2） | 车辆蓝牙主服务 |
| 读/通知特征值 | `8ec94e31-f315-4f60-9fb8-838830daea5X` | 车辆→手机 |
| 写特征值 | `8ec94e32-f315-4f60-9fb8-838830daea5X` | 手机→车辆，发指令 |

注：不同车型/模块后缀可能为 50/51/52，App 内可改。

### 指令帧格式（5AA5 协议）
```
5A A5 [长度 2字节] [命令字节] [数据...]
```
从字符串池提取到的帧（可靠常量，用于收发验证）：
- `5aa5000a050900000000011cd396`
- `5aa5000a06090000000000359096`
- `5aa5000a060900000000011cd396`
- 响应校验正则：`5aa5000a060900[0-9a-fA-F]{12}96`

### 车辆控制入口（CarCtrl）
App 内存在明确的车辆控制类：Power（电源）、Lock（锁车）、**Cushion（坐垫/坐桶）**、CallCar（寻车）、Ilg。

### 靠近自动开机机制（官方同款，已确认）
- 车辆蓝牙一直广播（iBeacon / 服务 UUID `8ec94e30`）
- App 后台扫描，读取 RSSI
- 字符串证据：
  - `Find the target device, but rssi is not in the range`
  - `mCurrentUnLockRssi` / `niuLinkUnLockValue`
  - `iBeacon data parsed: UUID=..., major=..., minor=..., power=...`
  - `onHandleNiuBeacon, mac=...`
- 逻辑：RSSI ≥ 解锁阈值 → 自动连接并解锁/开机；RSSI < 阈值 → 断开/关机

## 三、为什么开机/关机/开坐桶指令拿不到完整字节
开机/关机/开坐桶的**具体指令帧在加密的代码区**（网易易盾），静态无法直接读取。
（在 43~51MB 加密区可见 cmd=02 的鉴权帧，但字节分布异常、不可信。）

**解决办法（App 已内置支持）：**
1. 用 nRF Connect + 借用有主账号的手机抓取真实写入帧（详见使用说明）。
2. 在 App 探测模式下逐个尝试候选帧。
3. 老款/无系统车型可试旧协议帧：`5A C1 10 ... A5`（头码5A、尾码A5、XOR校验）。

## 四、其他发现
- 无系统车型用「NIU Link」蓝牙模块（蓝牙名 NIU Link 开头），走十进制字符串指令（CSDN 社区有解速/限速码案例）。
- WiFi 直连热点名格式：`DIRECT-NIU-Link-XXXX`。
- App 内含 iBeacon 解析与 RSSI 解锁逻辑，证明"靠近开机"是官方功能且可通过蓝牙直接复刻。
